public class Ternary {
    public static void main(String[] args) {
        int x, y;

        x = 51;

        y = (x == 51) ? 1 : 0;
        System.out.println("value of yes:" + y);

        y = (x == 1) ? 0 : 1;
        System.out.println("value of y is:" + y);
    }
}
